<Window x:Class="LibraryApp.View.UsersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryApp.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize" 
        Title="UsersView" Height="400" Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
        <Button Content="Legg til bruker"/>
        <Button Content="Slett bruker"/>
        <Button Content="Detaljer" Click="ButtonDetails_OnClick"/>
        <Button Content="Tilbake" Click="ButtonBack_OnClick"/>
        </StackPanel>
        <ScrollViewer Grid.Column="1" >
            <ListView x:Name="UserList" 
                      SelectionMode="Single" Width="320"/>
        </ScrollViewer>
    </Grid>
</Window>
